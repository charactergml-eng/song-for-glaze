# Server Configuration
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0

# CORS Configuration (set to your domain in production)
# Example: https://yourdomain.com
CORS_ORIGIN=*

# MongoDB Configuration
# Get your connection string from MongoDB Atlas: https://www.mongodb.com/cloud/atlas
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGODB_URI=

# OpenAI Configuration
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Vercel Blob Storage (get these from Vercel dashboard)
# https://vercel.com/dashboard/stores
BLOB_READ_WRITE_TOKEN=

# Vercel Analytics (optional - works automatically if deployed on Vercel)
# When deploying to other platforms, analytics still work but require the token
NEXT_PUBLIC_VERCEL_ANALYTICS_ID=
